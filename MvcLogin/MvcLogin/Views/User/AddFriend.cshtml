@model MvcLogin.Models.FriendModel

@{
    ViewBag.Title = "AddFriend";
}

<h2>AddFriend</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

<fieldset>
    <legend>ChatModel</legend>
    <div class="editor-label">
        @Html.LabelFor(model => model.userName)
    </div>
    <div class="editor-field">
        @Html.TextBoxFor(model => model.userName, new { @readonly = "readonly" })
        @Html.ValidationMessageFor(model => model.userName)
    </div>
    <div class="editor-label">
        @Html.LabelFor(model => model.userFriend)
    </div>
    <div class="editor-field">
        @Html.EditorFor(model => model.userFriend)
        @Html.ValidationMessageFor(model => model.userFriend)
    </div>

    <p>
        <input type="submit" value="Create" />
    </p>
</fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
